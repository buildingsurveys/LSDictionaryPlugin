const callbackDictLoopUpIcon = 'specIcon'; 

function specIcon() { 
 return `<img src="${assetsUrl}" alt="info-icon" width="500" height="600" />`; 
}

const configs = {
    termScanSelector: 'div.question-text, .answer-container, div.question-valid-container, div.question-help-container'
};
